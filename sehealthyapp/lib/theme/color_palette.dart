import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  static const Color primary = Color(0xFF2265E9);         // tombol awal, splash
  static const Color secondary = Color(0xFF2563EB);       // tombol login/register
  static const Color accentBlue = Color(0xFF2F60FF);      // link teks “Sign up”, “Log in”

  // Backgrounds
  static const Color bgLightBlue = Color(0xFFE9F2FF);     // FirstScreen, ChooseRoleScreen
  static const Color bgPaleBlue = Color(0xFFEFF6FF);      // Login, Onboarding, etc
  static const Color bgSkyBlue = Color(0xFFE8F0FE);       // TBC screening
  static const Color bgSoftBlue = Color(0xFFE6F0FF);      // Profile screen
  static const Color bgSchedule = Color(0xFFF5F7FA);      // Schedule screen

  // Cards & Components
  static const Color cardBlue = Color(0xFFDBEAFE);        // Health Checkup
  static const Color cardGreen = Color(0xFFD1FAE5);       // TBC Screening
  static const Color cardPurple = Color(0xFFEDE9FE);      // Health History
  static const Color cardYellow = Color(0xFFFEF9C3);      // Resources
  static const Color cardLightBlue = Color(0xFFDDEEFF);   // Upcoming Checkup
  static const Color cardIconBlue = Color(0xFFE3F2FD);    // Icon bulat TBC questions

  // Buttons (States)
  static const Color yesSelected = Color(0xFF90CAF9);     // Blue "Yes"
  static const Color noSelected = Color(0xFFEF9A9A);      // Red "No"
  static const Color darkBlue = Color(0xFF1565C0);        // Submit button (TBC)

  // Feedback
  static const Color dangerRed = Colors.red;              // Log Out button

  // Text
  static const Color textGrey = Colors.grey;              // subtitle, label, hints
  static const Color textDarkGrey = Colors.black54;       // subtitle, info
  static const Color textDarker = Colors.black87;         // title, heading

  // Basics
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}
